function showAlert() {
  alert("Button clicked");
}

// Attach the showAlert function to the global scope
window.showAlert = showAlert;

function displayName() {
  alert("Muhammad Adam");
}

function sum(a, b) {
  return a + b;
}
